# vulnMaster debug build - win11

npm run tauri dev

Note: The app is a "shell" that connects to a live web server (http://localhost:1420) so that when you save code, the window updates instantly. 
If the server stops, the app breaks.

Do not use debug build for deleivery

# vulnMaster release build - win11
`C:\Projects\vulnMaster\src-tauri\target\release`

npm run tauri build